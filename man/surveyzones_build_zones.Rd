% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zones.R
\name{surveyzones_build_zones}
\alias{surveyzones_build_zones}
\title{Build Zones for Survey Tracts}
\usage{
surveyzones_build_zones(
  sparse_distances,
  tracts,
  D_max,
  max_workload_per_zone = Inf,
  enforce_partition = TRUE,
  solver = "glpk",
  max_time = 300,
  rel_tol = 0.01,
  verbose = FALSE,
  strategy = "auto",
  use_cache = TRUE,
  access_points = NULL
)
}
\arguments{
\item{sparse_distances}{A tibble of sparse distances as
returned by \code{\link[=surveyzones_compute_sparse_distances]{surveyzones_compute_sparse_distances()}} or
\code{\link[=surveyzones_precomputed_distances]{surveyzones_precomputed_distances()}}.}

\item{tracts}{A data.frame with columns \code{tract_id} and
\code{expected_service_time}.  Optionally \code{partition_id}.}

\item{D_max}{Numeric scalar.  Maximum distance.  Pairs in
\code{sparse_distances} with \code{distance > D_max} are dropped
before solving.}

\item{max_workload_per_zone}{Numeric scalar.  Upper bound on
the sum of \code{expected_service_time} within any zone.  Defaults
to \code{Inf} (uncapacitated).}

\item{enforce_partition}{Logical.  Whether to split by
\code{partition_id} (default \code{TRUE}).}

\item{solver}{Character scalar.  Which solver backend to use.
MILP solvers: \code{"glpk"} (default), \code{"highs"}, \code{"cbc"}, \code{"symphony"}
(requires the corresponding ROI plugin, e.g. \code{ROI.plugin.highs}).
Use \code{"spopt"} to delegate to \code{\link[spopt:allocate_zones]{spopt::allocate_zones()}} which uses
a Rust/HiGHS backend (much faster for large problems; requires the
\code{spopt} package).}

\item{max_time}{Numeric scalar.  Maximum seconds to spend solving each
partition. Default \code{300}.}

\item{rel_tol}{Numeric scalar.  Relative tolerance for solver convergence.
Default \code{0.01}.}

\item{verbose}{Logical.  Print solver progress messages? Default \code{FALSE}.}

\item{strategy}{Character scalar.  Solving strategy for capacitated problems
(when \code{max_workload_per_zone} is finite).  \code{"auto"} (default) uses
\code{"uncap_then_split"} when \code{max_workload_per_zone} is finite and \code{"direct"}
otherwise.  \code{"uncap_then_split"} solves the uncapacitated p-median first
(fast), then recursively splits any zone that exceeds the workload cap into
smaller zones -- each split sub-problem is tiny and solved instantly.
\code{"direct"} uses the capacitated MILP directly (may be slow for large
instances).}

\item{use_cache}{Logical.  Whether to use the disk cache (default \code{TRUE}).
Set to \code{FALSE} to force re-computation.}

\item{access_points}{Optional sf object with POINT geometries and a
\code{tract_id} column.  Stored in the returned plan so that downstream
functions like \code{\link[=surveyzones_sequence]{surveyzones_sequence()}} can use it without
the caller having to pass it again.}
}
\value{
A \code{surveyzones_plan} object.
}
\description{
Top-level entry point that partitions tracts (optionally by
jurisdiction), solves a p-median problem for each partition,
and returns a combined \code{surveyzones_plan}.
}
\details{
When \code{max_workload_per_zone} is finite, solves a \strong{capacitated}
p-median (zones cannot exceed the workload cap).
When \code{max_workload_per_zone = Inf} (the default), solves an
\strong{uncapacitated} p-median (no workload constraints, fewer MILP
constraints, tighter LP relaxation, faster solve times).

Results are cached to disk by default (\code{use_cache = TRUE}): repeated calls
with identical arguments return instantly without re-running the solver.
Use \code{surveyzones_clear_cache()} to wipe the cache.
}
