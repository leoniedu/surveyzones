% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{surveyzones_engine_osrm}
\alias{surveyzones_engine_osrm}
\title{Create an OSRM Distance Engine}
\usage{
surveyzones_engine_osrm(
  measure = c("duration", "distance"),
  osrm.server = NULL,
  osrm.profile = NULL
)
}
\arguments{
\item{measure}{Character scalar.  One of \code{"duration"} (default, in
minutes) or \code{"distance"} (in meters).}

\item{osrm.server}{OSRM server URL.  Defaults to the \code{osrm} package
option (the public demo server).}

\item{osrm.profile}{Routing profile.  Defaults to the \code{osrm} package
option (typically \code{"car"}).}
}
\value{
A function with signature
\verb{function(origins, destinations)} that returns an n x m numeric
matrix of travel times (minutes) or distances (meters).
}
\description{
Returns a function that computes road-network travel times or distances
using the OSRM routing service via \code{\link[osrm:osrmTable]{osrm::osrmTable()}}.
}
\details{
The public OSRM demo server has rate limits and should not be used
for large-scale computation.
For production use, set up a local OSRM server and pass its URL
via the \code{osrm.server} argument.
}
