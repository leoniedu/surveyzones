% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distances.R
\name{surveyzones_complete_distances}
\alias{surveyzones_complete_distances}
\title{Complete a Sparse Distance Table with Haversine Fill-In}
\usage{
surveyzones_complete_distances(
  sparse_distances,
  access_points,
  speed_kmh = 0.1
)
}
\arguments{
\item{sparse_distances}{A tibble with columns \code{origin_id},
\code{destination_id}, and \code{distance}.}

\item{access_points}{An sf object with POINT geometries and a
\code{tract_id} column.}

\item{speed_kmh}{Numeric scalar.  Assumed travel speed in km/h for
converting haversine distance to time.  Default \code{0.1} (intentionally
harsh — missing pairs likely represent real barriers).}
}
\value{
A tibble with the same schema as \code{sparse_distances}, with
missing pairs filled in.  Self-pairs are excluded.
}
\description{
For every directed pair of tract IDs in \code{access_points} not already
present in \code{sparse_distances}, computes haversine (great-circle) distance
in km and converts to the distance unit using a speed parameter:
\code{distance = haversine_km / speed_kmh * 60} (result in minutes).
}
\details{
Existing pairs are preserved unchanged.  Use this before
\code{\link[=surveyzones_sequence_zones]{surveyzones_sequence_zones()}} with \code{method = "mds"} when the
sparse distance table may be incomplete.
}
